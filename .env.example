# AcoustiGuard API Environment Configuration Example

# Base URL for the Python ML inference backend
VITE_API_BASE_URL=http://localhost:8000

# Set to true to use mock inference when Python backend is offline
VITE_USE_MOCK_INFERENCE=true
